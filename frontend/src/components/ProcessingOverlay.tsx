import { Loader2 } from "lucide-react";

interface ProcessingOverlayProps {
  open: boolean;
  message?: string;
}

export function ProcessingOverlay({ open }: ProcessingOverlayProps) {
  return null;
}
